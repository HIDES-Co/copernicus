[project]
name = "openeo_project"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "folium>=0.20.0",
    "openeo>=0.45.0",
]

[project.optional-dependencies]
dev = [
    "ipykernel>=7.0.1",
]
