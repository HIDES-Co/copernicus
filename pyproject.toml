[project]
name = "openeo_project"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "folium>=0.20.0",
    "openeo>=0.45.0",
]

[dependency-groups]
dev = [
    "ipykernel>=7.0.1",
]
